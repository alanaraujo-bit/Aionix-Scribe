<Window x:Class="AionixScribe.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aionix Scribe — Configurações"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#14141E"
        Foreground="White"
        FontFamily="Segoe UI">
    <StackPanel Margin="24">
        <TextBlock Text="Atalho global" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBlock Text="Pressione o atalho uma vez para começar a ditar, e de novo para parar."
                   Foreground="#9CA3AF" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,16"/>

        <Border Background="#1E1E2A" CornerRadius="8" Padding="16" Margin="0,0,0,12">
            <TextBlock x:Name="CurrentHotkeyText" Text="Ctrl+Alt+Espaço" FontSize="20" FontWeight="Bold"
                       HorizontalAlignment="Center" Foreground="#4ADE80"/>
        </Border>

        <TextBlock x:Name="StatusText" FontSize="12" Foreground="#F87171" Margin="0,0,0,12" TextWrapping="Wrap" Visibility="Collapsed"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
            <Button x:Name="ChangeButton" Content="Alterar atalho" Click="OnChangeClicked"
                    Padding="12,6" Margin="0,0,8,0" Background="#2D2D3D" Foreground="White" BorderThickness="0"/>
            <Button x:Name="ResetButton" Content="Restaurar padrão" Click="OnResetClicked"
                    Padding="12,6" Margin="0,0,8,0" Background="#2D2D3D" Foreground="White" BorderThickness="0"/>
            <Button Content="Fechar" Click="OnCloseClicked" Padding="12,6" Background="#2D2D3D" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </StackPanel>
</Window>
