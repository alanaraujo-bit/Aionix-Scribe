<Window x:Class="AionixScribe.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="Height"
        Width="420"
        FontFamily="./Fonts/#Sora"
        KeyDown="OnKeyDown">
    <Border CornerRadius="16"
            Background="{DynamicResource CardBackgroundBrush}"
            BorderBrush="{DynamicResource BorderStrongBrush}"
            BorderThickness="1"
            Padding="24,22">
        <StackPanel>
            <TextBlock x:Name="TitleText" Style="{StaticResource SectionTitleTextStyle}" FontSize="16"
                       TextWrapping="Wrap" Margin="0,0,0,8"/>
            <TextBlock x:Name="MessageText" Style="{StaticResource BodyMutedTextStyle}" FontSize="12.5"
                       TextWrapping="Wrap" LineHeight="19" Margin="0,0,0,22"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton" Content="Cancelar" Style="{StaticResource GhostButtonStyle}"
                        Margin="0,0,10,0" Click="OnCancel" IsCancel="True"/>
                <Button x:Name="ConfirmButton" Content="Confirmar" Style="{StaticResource PrimaryButtonStyle}"
                        Click="OnConfirm"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
